# fail2ban filter for failed Signage superadmin logins
# Matches a 401 response on POST /api/v1/superadmin/auth/login in the nginx access log.
[Definition]
failregex = ^<HOST> .* "POST /api/v1/superadmin/auth/login HTTP[^"]*" 401
ignoreregex =
