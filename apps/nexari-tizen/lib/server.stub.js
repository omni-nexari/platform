/**
 * Nexari Player — MDC Bridge stub (Samsung-signed).
 *
 * All MDC/HTTP logic lives in ../js/mdc.js — update that file freely
 * without re-signing. Only THIS stub file is submitted to Samsung for signing.
 *
 * To obtain a signed version: submit THIS file to the Samsung SSSP Developer Portal
 * for each SSSP generation (SSSP4=2016, SSSP5=2017, SSSP6=2018, SSSP7=2019, SSSP8=2022).
 * All signed variants are byte-for-byte identical — only the signing key differs.
 */
require('../js/mdc.js')();
